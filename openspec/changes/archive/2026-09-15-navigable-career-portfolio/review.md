# Review

- Reviewer: Codex quick review
- Reviewed: 2026-09-15
- Outcome: APPROVED

## Findings

The change keeps navigation inside the Publication projection and requires explicit semantic relationships for traversal. Stable fragment links and deliberate explore controls improve pointer, keyboard, assistive-technology, and browser context-menu use without making ordinary row selection disruptive or creating a new source of truth. The complete default prevents accidental career truncation while the preview makes deliberate shorter formats possible. The versioned template contract and fictional generator fixture reduce presentation drift, while `featured` remains an approved semantic decision rather than a visual inference.

## Architecture check

The domain owns the projection option and renderer port. The static HTML implementation remains an outbound adapter consuming an immutable release. No adapter reads canonical storage, and no graph database is introduced.
