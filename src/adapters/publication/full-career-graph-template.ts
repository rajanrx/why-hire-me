/** The complete-graph interaction is bundled locally; it never loads a graph library or remote asset. */
export const fullCareerGraphStyles = `.graph-shell{position:relative;padding:12px;border:1px solid var(--line);border-radius:9px;background:var(--surface)}.graph-toolbar{display:flex;align-items:flex-start;justify-content:space-between;gap:18px;flex-wrap:wrap;margin:0 0 12px}.graph-focus-picker{position:relative;flex:1;min-width:min(100%,300px)}.graph-focus-picker label{display:block;margin-bottom:5px;font-size:11px;font-weight:700;color:var(--muted)}.graph-focus-picker input{width:100%;height:36px;padding:0 11px;border:1px solid var(--line);border-radius:6px;background:#fff}.graph-focus-options{position:absolute;top:60px;left:0;z-index:8;width:min(100%,440px);max-height:280px;overflow:auto;padding:5px;border:1px solid var(--line);border-radius:6px;background:var(--surface);box-shadow:var(--shadow)}.graph-focus-options[hidden]{display:none}.graph-focus-options button{display:flex;width:100%;align-items:center;justify-content:space-between;gap:8px;padding:8px;border:0;border-radius:4px;background:transparent;text-align:left;cursor:pointer}.graph-focus-options button:hover,.graph-focus-options button:focus-visible{background:var(--blue-soft)}.graph-focus-options button[aria-selected=true]{color:var(--blue)}.graph-focus-options small{color:var(--muted)}.graph-focus-chips{display:flex;gap:6px;flex-wrap:wrap;margin-top:7px}.graph-focus-chips button{padding:4px 7px;border:1px solid #cbd9e4;border-radius:5px;background:#f6f9fc;font-size:11px;cursor:pointer}.graph-actions{display:flex;gap:7px;align-items:center;flex-wrap:wrap}.graph-actions button{min-height:36px;padding:6px 10px;border:1px solid var(--line);border-radius:5px;background:var(--surface);cursor:pointer}.graph-actions button:hover{border-color:var(--blue)}.graph-layout{grid-template-columns:minmax(0,1fr) minmax(260px,310px);gap:12px}.graph-wrap{height:clamp(460px,65svh,720px);min-height:0;overflow:hidden;touch-action:none;cursor:grab;background:radial-gradient(circle at 50% 48%,#fff,#fafbfc)}.graph-wrap.is-panning{cursor:grabbing}.graph{min-width:0;width:100%;height:100%;user-select:none}.graph-world .graph-edge{stroke:#bbc8d3;stroke-width:1.3;vector-effect:non-scaling-stroke;transition:opacity .12s ease}.graph-world .graph-edge.is-quiet{opacity:.12}.graph-world .graph-node{cursor:pointer;transition:opacity .12s ease}.graph-world .graph-node.is-quiet{opacity:.18}.graph-world .graph-node circle{fill:#fff;stroke:#8e9aaa;stroke-width:1.5;vector-effect:non-scaling-stroke}.graph-world .graph-node[data-type=Organisation] circle,.graph-world .graph-node[data-type=Engagement] circle{fill:#eaf3fb;stroke:#1769aa}.graph-world .graph-node[data-type=Work] circle,.graph-world .graph-node[data-type=Contribution] circle{fill:#edf7f2;stroke:#26735c}.graph-world .graph-node.is-focused circle,.graph-world .graph-node.is-selected circle,.graph-world .graph-node:hover circle,.graph-world .graph-node:focus-visible circle{stroke:#1769aa;stroke-width:3}.graph-world .graph-node text{fill:var(--ink);font-size:11px;font-weight:600;paint-order:stroke;stroke:#fff;stroke-width:3px;stroke-linejoin:round}.graph-world .graph-node:not(.is-major):not(.is-focused):not(.is-selected):not(.is-hovered) text{opacity:0}.graph-world.is-zoomed .graph-node:not(.is-quiet) text{opacity:1}.graph-shell #graph-inspector{top:0;max-height:clamp(460px,65svh,720px);overflow:auto}.graph-shell.is-fullscreen,.graph-shell:fullscreen{position:fixed;z-index:35;inset:0;display:flex;flex-direction:column;width:100vw;height:100svh;margin:0;border:0;border-radius:0;padding:12px;background:var(--surface)}.graph-shell.is-fullscreen .graph-layout,.graph-shell:fullscreen .graph-layout{flex:1;min-height:0}.graph-shell.is-fullscreen .graph-wrap,.graph-shell:fullscreen .graph-wrap{height:100%;min-height:0}.graph-shell.is-fullscreen #graph-inspector,.graph-shell:fullscreen #graph-inspector{max-height:100%;overflow:auto}@media(max-width:820px){.graph-layout{grid-template-columns:1fr}.graph-wrap{height:min(62svh,560px)}.graph-shell #graph-inspector{max-height:none}.graph-shell.is-fullscreen .graph-wrap,.graph-shell:fullscreen .graph-wrap{height:100%}.graph-shell .graph-layout #graph-inspector{max-height:min(78svh,680px)}}@media(prefers-reduced-motion:reduce){.graph-world .graph-node,.graph-world .graph-edge{transition:none}}@media print{.graph-shell{display:none!important}}`;

/** Inserted into the renderer's existing local script after the shared career/entity setup. */
export const fullCareerGraphApp = String.raw`
const graph=document.querySelector("#graph"),focus=document.querySelector("#graph-focus"),focusOptions=document.querySelector("#graph-focus-options"),focusChips=document.querySelector("#graph-focus-chips"),focusClear=document.querySelector("#graph-focus-clear"),graphFit=document.querySelector("#graph-fit"),graphFull=document.querySelector("#graph-fullscreen"),graphShell=document.querySelector("#graph-shell"),graphWrap=document.querySelector(".graph-wrap"),graphPanel=document.querySelector("#graph-inspector");
const graphItems=[...items.values()].sort((a,b)=>a.type.localeCompare(b.type)||a.name.localeCompare(b.name)||a.id.localeCompare(b.id));
const graphIds=new Set(graphItems.map(item=>item.id));
const graphRelations=relations.filter(relation=>graphIds.has(relation.subject)&&graphIds.has(relation.object));
const graphAdj=new Map(graphItems.map(item=>[item.id,new Set([item.id])]));
graphRelations.forEach(relation=>{graphAdj.get(relation.subject).add(relation.object);graphAdj.get(relation.object).add(relation.subject)});
const graphDegrees=graphItems.map(item=>Math.max(0,graphAdj.get(item.id).size-1)).sort((a,b)=>a-b);
const typicalDegree=graphDegrees[Math.floor(graphDegrees.length/2)]||0,maxDegree=graphDegrees.at(-1)||0;
function nodeRadiusFactor(degree){if(maxDegree<=typicalDegree||degree<=typicalDegree)return 1;const floor=Math.log1p(typicalDegree),span=Math.log1p(maxDegree)-floor;return 1+.65*Math.min(1,Math.max(0,(Math.log1p(degree)-floor)/span))}
const graphTypeTier={Organisation:0,Engagement:1,Role:1,Work:2,Contribution:3,TechnologyUse:3,"Reported outcome":3,Technology:4,Evidence:4};
function graphLayout(){
  const unseen=new Set(graphItems.map(item=>item.id)),components=[];
  while(unseen.size){const root=[...unseen][0],queue=[root],component=[];unseen.delete(root);for(let i=0;i<queue.length;i++){const id=queue[i];component.push(id);for(const next of graphAdj.get(id))if(unseen.delete(next))queue.push(next)}components.push(component)}
  components.sort((a,b)=>b.length-a.length||a[0].localeCompare(b[0]));
  const positions=new Map();let xCursor=0,yCursor=0,rowHeight=0;const rowLimit=1700;
  for(const component of components){
    const local=new Map(),tiers=new Map();
    for(const id of component){const item=items.get(id),tier=graphTypeTier[item.type]??4;if(!tiers.has(tier))tiers.set(tier,[]);tiers.get(tier).push(id)}
    for(const [tier,ids] of tiers){ids.sort();ids.forEach((id,index)=>{let hash=0;for(const char of id)hash=(hash*31+char.charCodeAt(0))>>>0;const angle=(index/ids.length)*Math.PI*2+(hash%17)*.012,radius=tiers.size===1?Math.max(45,ids.length*12):42+tier*78+Math.sqrt(component.length)*4;local.set(id,{x:Math.cos(angle)*radius,y:Math.sin(angle)*radius,vx:0,vy:0})})}
    const edges=graphRelations.filter(relation=>local.has(relation.subject)&&local.has(relation.object));
    const rounds=component.length>180?35:component.length>90?55:95,pairWindow=component.length>240?36:component.length;
    for(let round=0;round<rounds;round++){
      for(let a=0;a<component.length;a++)for(let b=a+1;b<Math.min(component.length,a+pairWindow);b++){
        const p=local.get(component[a]),q=local.get(component[b]),dx=p.x-q.x,dy=p.y-q.y,dist2=dx*dx+dy*dy+160;
        const push=1300/dist2,fx=dx*push,fy=dy*push;p.vx+=fx;p.vy+=fy;q.vx-=fx;q.vy-=fy;
      }
      for(const edge of edges){const p=local.get(edge.subject),q=local.get(edge.object),dx=q.x-p.x,dy=q.y-p.y,dist=Math.hypot(dx,dy)||1,pull=(dist-105)*.0035,fx=dx/dist*pull,fy=dy/dist*pull;p.vx+=fx;p.vy+=fy;q.vx-=fx;q.vy-=fy}
      for(const id of component){const p=local.get(id);p.vx=(p.vx-p.x*.0008)*.64;p.vy=(p.vy-p.y*.0008)*.64;p.x+=p.vx;p.y+=p.vy}
    }
    let minX=Infinity,minY=Infinity,maxX=-Infinity,maxY=-Infinity;for(const id of component){const p=local.get(id);minX=Math.min(minX,p.x);minY=Math.min(minY,p.y);maxX=Math.max(maxX,p.x);maxY=Math.max(maxY,p.y)}const width=maxX-minX+130,height=maxY-minY+130;
    if(xCursor&&xCursor+width>rowLimit){xCursor=0;yCursor+=rowHeight;rowHeight=0}
    for(const id of component){const p=local.get(id);positions.set(id,{x:p.x-minX+xCursor+65,y:p.y-minY+yCursor+65})}
    xCursor+=width+35;rowHeight=Math.max(rowHeight,height+35);
  }
  return positions;
}
const graphPositions=graphLayout(),world=document.createElementNS("http://www.w3.org/2000/svg","g");world.setAttribute("class","graph-world");graph.appendChild(world);
const nodeElements=new Map(),nodeParts=new Map(),edgeElements=[];
for(const relation of graphRelations){const a=graphPositions.get(relation.subject),b=graphPositions.get(relation.object),line=document.createElementNS("http://www.w3.org/2000/svg","line");line.setAttribute("class","graph-edge");for(const [key,value] of Object.entries({x1:a.x,y1:a.y,x2:b.x,y2:b.y}))line.setAttribute(key,String(value));line.dataset.from=relation.subject;line.dataset.to=relation.object;const title=document.createElementNS("http://www.w3.org/2000/svg","title");title.textContent=items.get(relation.subject).name+" → "+items.get(relation.object).name+" · "+relation.predicate;line.appendChild(title);world.appendChild(line);edgeElements.push(line)}
for(const item of graphItems){const p=graphPositions.get(item.id),major=graphTypeTier[item.type]<=2,degree=Math.max(0,graphAdj.get(item.id).size-1),countLabel=degree+" "+(degree===1?"connection":"connections"),group=document.createElementNS("http://www.w3.org/2000/svg","g");group.setAttribute("class","graph-node"+(major?" is-major":"")+(item.inclusion?.status==="featured"?" featured":""));group.setAttribute("role","button");group.setAttribute("tabindex","0");group.setAttribute("aria-label",item.name+" · "+item.type+" · "+countLabel+"; Enter reads this node, Shift Enter explores it");group.dataset.node=item.id;group.dataset.type=item.type;const circle=document.createElementNS("http://www.w3.org/2000/svg","circle");circle.setAttribute("cx",String(p.x));circle.setAttribute("cy",String(p.y));const label=document.createElementNS("http://www.w3.org/2000/svg","text");label.textContent=item.name.length>38?item.name.slice(0,36)+"…":item.name;const title=document.createElementNS("http://www.w3.org/2000/svg","title");title.textContent=item.name+" · "+item.type+" · "+countLabel;group.append(circle,label,title);world.appendChild(group);nodeElements.set(item.id,group);nodeParts.set(item.id,{circle,label,p,major,degree})}
let graphScale=1,graphX=0,graphY=0,selectedNode=null,clickedFocus=null,hoverNode=null,focusIds=new Set(),pan=null,press=null;
const viewport={width:1200,height:800};
function graphPoint(event){const rect=graph.getBoundingClientRect();return{x:(event.clientX-rect.left)*viewport.width/Math.max(rect.width,1),y:(event.clientY-rect.top)*viewport.height/Math.max(rect.height,1)}}
function sizeNodes(){const width=graph.getBoundingClientRect().width;if(width<1)return;const unit=viewport.width/width/graphScale;for(const part of nodeParts.values()){const radius=(part.major?10:6.5)*nodeRadiusFactor(part.degree);part.circle.setAttribute("r",String(radius*unit));part.label.setAttribute("x",String(part.p.x+(radius+6)*unit));part.label.setAttribute("y",String(part.p.y+4*unit));part.label.style.fontSize=(part.major?12.5:11.5)*unit+"px"}}
function transformGraph(){world.setAttribute("transform","translate("+graphX+" "+graphY+") scale("+graphScale+")");world.classList.toggle("is-zoomed",graphScale>1.35);sizeNodes()}
function fitGraph(){if(!graphItems.length)return;let minX=Infinity,minY=Infinity,maxX=-Infinity,maxY=-Infinity;for(const p of graphPositions.values()){minX=Math.min(minX,p.x);minY=Math.min(minY,p.y);maxX=Math.max(maxX,p.x);maxY=Math.max(maxY,p.y)}minX-=45;minY-=45;maxX+=190;maxY+=45;graphScale=Math.max(.12,Math.min(2,Math.min(viewport.width/(maxX-minX),viewport.height/(maxY-minY))));graphX=(viewport.width-(maxX-minX)*graphScale)/2-minX*graphScale;graphY=(viewport.height-(maxY-minY)*graphScale)/2-minY*graphScale;transformGraph()}
function activeNeighbours(){const roots=focusIds.size?focusIds:clickedFocus?new Set([clickedFocus]):new Set();if(!roots.size)return null;const shown=new Set();for(const id of roots)for(const neighbour of graphAdj.get(id)||[])shown.add(neighbour);return shown}
function highlightGraph(){const near=activeNeighbours();for(const [id,node] of nodeElements){node.classList.toggle("is-quiet",!!near&&!near.has(id)&&id!==selectedNode);node.classList.toggle("is-focused",focusIds.has(id)||clickedFocus===id);node.classList.toggle("is-selected",selectedNode===id);node.classList.toggle("is-hovered",hoverNode===id)}for(const edge of edgeElements)edge.classList.toggle("is-quiet",!!near&&!(near.has(edge.dataset.from)&&near.has(edge.dataset.to)))}
function readNode(id,trigger,makeClickFocus=false){const item=items.get(id);if(!item)return;selectedNode=id;if(makeClickFocus&&!focusIds.size){clickedFocus=id;renderFocusChips()}const count=Math.max(0,(graphAdj.get(id)?.size||1)-1);graphPanel.dataset.selectedId=id;graphPanel.innerHTML='<p class="overline">Selected node · '+esc(item.type)+'</p><h3>'+entity(id,item.name)+'</h3><p class="focused-summary">'+esc(item.summary||"No entity-specific narrative is present; explore linked records for context.")+'</p>'+(item.details.length?'<dl>'+item.details.map(detail=>'<div><dt>'+esc(detail.label)+'</dt><dd>'+esc(detail.value)+'</dd></div>').join("")+'</dl>':"")+'<p class="meta">'+count+' connected '+(count===1?'record':'records')+'</p><div class="connections">'+(connections(id)||'<p>No explicit relationships connect this node.</p>')+'</div>';highlightGraph();graphPanel.dispatchEvent(new CustomEvent("graph-selection-change",{bubbles:true,detail:{id,trigger}}))}
function renderFocusChips(){focusChips.innerHTML=[...focusIds].map(id=>'<button type="button" data-remove-focus="'+esc(id)+'" aria-label="Remove '+esc(items.get(id).name)+' from focus">'+esc(items.get(id).name)+' ×</button>').join("");focusClear.disabled=!focusIds.size&&!clickedFocus;renderFocusOptions()}
function renderFocusOptions(){const query=focus.value.toLowerCase().trim(),matches=graphItems.filter(item=>!query||item.name.toLowerCase().includes(query)||item.id.toLowerCase().includes(query)||item.type.toLowerCase().includes(query));focusOptions.innerHTML=matches.map(item=>'<button type="button" role="option" aria-selected="'+focusIds.has(item.id)+'" data-focus-option="'+esc(item.id)+'"><span>'+esc(item.name)+'</span><small>'+esc(item.type)+'</small></button>').join("")||'<p>No matching career records.</p>';focusOptions.hidden=document.activeElement!==focus&&!focusOptions.contains(document.activeElement);focus.setAttribute("aria-expanded",String(!focusOptions.hidden))}
function addFocus(id,trigger){if(!items.has(id))return;focusIds.add(id);clickedFocus=null;renderFocusChips();readNode(id,trigger||focus,false)}
function toggleFocus(id,trigger){const adding=!focusIds.has(id);if(!adding){focusIds.delete(id);clickedFocus=null;renderFocusChips();highlightGraph()}else addFocus(id,trigger);focus.value="";renderFocusOptions();if(!adding||!window.matchMedia("(max-width: 820px)").matches)focus.focus()}
function clearFocus(){focusIds.clear();clickedFocus=null;renderFocusChips();highlightGraph()}
focus.addEventListener("focus",()=>{focusOptions.hidden=false;focus.setAttribute("aria-expanded","true");renderFocusOptions()});focus.addEventListener("input",()=>{focusOptions.hidden=false;renderFocusOptions()});focus.addEventListener("keydown",event=>{if(event.key==="Escape"){focusOptions.hidden=true;focus.setAttribute("aria-expanded","false");return}if(event.key==="ArrowDown"){event.preventDefault();focusOptions.querySelector("[data-focus-option]")?.focus()}if(event.key==="Enter"){const first=focusOptions.querySelector("[data-focus-option]");if(first){event.preventDefault();toggleFocus(first.dataset.focusOption,focus)}}});
focusOptions.addEventListener("click",event=>{const option=event.target.closest("[data-focus-option]");if(option)toggleFocus(option.dataset.focusOption,option)});focusOptions.addEventListener("keydown",event=>{if(event.key==="Escape"){focusOptions.hidden=true;focus.setAttribute("aria-expanded","false");focus.focus()}else if(event.key==="ArrowDown"||event.key==="ArrowUp"){event.preventDefault();const options=[...focusOptions.querySelectorAll("[data-focus-option]")],current=options.indexOf(document.activeElement),next=options[Math.max(0,Math.min(options.length-1,current+(event.key==="ArrowDown"?1:-1)))];next?.focus()}});
focusChips.addEventListener("click",event=>{const remove=event.target.closest("[data-remove-focus]");if(remove){focusIds.delete(remove.dataset.removeFocus);clickedFocus=null;renderFocusChips();highlightGraph();focus.focus()}});focusClear.addEventListener("click",clearFocus);document.addEventListener("click",event=>{if(!event.target.closest(".graph-focus-picker")){focusOptions.hidden=true;focus.setAttribute("aria-expanded","false")}});
graphFit.addEventListener("click",()=>{fitGraph();clearFocus()});
graph.addEventListener("wheel",event=>{event.preventDefault();const p=graphPoint(event),next=Math.max(.12,Math.min(8,graphScale*Math.exp(-event.deltaY*.0012))),ratio=next/graphScale;graphX=p.x-(p.x-graphX)*ratio;graphY=p.y-(p.y-graphY)*ratio;graphScale=next;transformGraph()},{passive:false});
function exploreGraphNode(id){if(graphShell.classList.contains("is-fullscreen"))exitFull();open(id)}
graph.addEventListener("pointerdown",event=>{const node=event.target.closest("[data-node]");if(node){press={id:node.dataset.node,x:event.clientX,y:event.clientY,timer:setTimeout(()=>{exploreGraphNode(node.dataset.node);press=null},550)};return}const p=graphPoint(event);pan={x:p.x,y:p.y,startX:graphX,startY:graphY};graphWrap.classList.add("is-panning");graph.setPointerCapture(event.pointerId)});
graph.addEventListener("pointermove",event=>{if(press&&(Math.abs(event.clientX-press.x)>8||Math.abs(event.clientY-press.y)>8)){clearTimeout(press.timer);press=null}if(pan){const p=graphPoint(event);graphX=pan.startX+p.x-pan.x;graphY=pan.startY+p.y-pan.y;transformGraph()}});
function endPointer(){if(press){clearTimeout(press.timer);press=null}pan=null;graphWrap.classList.remove("is-panning")};graph.addEventListener("pointerup",endPointer);graph.addEventListener("pointercancel",endPointer);
graph.addEventListener("click",event=>{const node=event.target.closest("[data-node]");if(node)readNode(node.dataset.node,node,true)});graph.addEventListener("dblclick",event=>{const node=event.target.closest("[data-node]");if(node){event.preventDefault();exploreGraphNode(node.dataset.node)}});graph.addEventListener("contextmenu",event=>{const node=event.target.closest("[data-node]");if(node){event.preventDefault();exploreGraphNode(node.dataset.node)}});
graph.addEventListener("mouseover",event=>{const node=event.target.closest("[data-node]");if(node&&hoverNode!==node.dataset.node){hoverNode=node.dataset.node;highlightGraph()}});graph.addEventListener("mouseout",event=>{const node=event.target.closest("[data-node]");if(node&&hoverNode===node.dataset.node){hoverNode=null;highlightGraph()}});
graph.addEventListener("keydown",event=>{const node=event.target.closest("[data-node]");if(node&&(event.key==="Enter"||event.key===" ")){event.preventDefault();if(event.shiftKey)exploreGraphNode(node.dataset.node);else readNode(node.dataset.node,node,true)}});
function paintFull(){const on=graphShell.classList.contains("is-fullscreen");graphFull.textContent=on?"Exit full screen":"Full screen";graphFull.setAttribute("aria-pressed",String(on))}
function enterFull(){graphShell.classList.add("is-fullscreen");paintFull();const request=graphShell.requestFullscreen?.bind(graphShell);if(request)Promise.resolve(request()).catch(()=>{})}
function exitFull(){graphShell.classList.remove("is-fullscreen");paintFull();if(document.fullscreenElement===graphShell)document.exitFullscreen?.().catch(()=>{})}
graphFull.addEventListener("click",()=>graphShell.classList.contains("is-fullscreen")?exitFull():enterFull());document.addEventListener("fullscreenchange",()=>{if(!document.fullscreenElement){graphShell.classList.remove("is-fullscreen");paintFull()}});document.addEventListener("keydown",event=>{if(event.key==="Escape"&&graphShell.classList.contains("is-fullscreen")&&!graphPanel.classList.contains("is-mobile-open")&&!document.querySelector("#drawer.open"))exitFull()});
graphShell.addEventListener("click",event=>{if(event.target.closest("[data-entity]")&&graphShell.classList.contains("is-fullscreen"))exitFull()});
document.addEventListener("click",event=>{const control=event.target.closest("[data-focus-graph]");if(!control)return;const id=control.dataset.focusGraph;close();setView("graph-view");addFocus(id,control);history.replaceState(null,"","#graph-view")});
document.querySelector("#evidence-search").addEventListener("input",event=>document.querySelectorAll("#evidence-body tr").forEach(row=>row.hidden=!row.textContent.toLowerCase().includes(event.target.value.toLowerCase())));
graphPanel.innerHTML='<p class="overline">Career graph</p><h3>Select a node</h3><p>All '+graphItems.length+' authorised records and '+graphRelations.length+' explicit relationships are shown. Click a node for its specific context, or type above to highlight several focus records.</p>';
renderFocusChips();fitGraph();if("ResizeObserver" in window)new ResizeObserver(sizeNodes).observe(graphWrap);
`;

export const mobileCareerGraphDetailApp = String.raw`
(()=>{const panel=document.querySelector("#graph-inspector"),shell=document.querySelector("#graph-shell"),tabs=document.querySelector(".tabs"),small=window.matchMedia("(max-width: 820px)");if(!panel||!shell)return;let returnFocus=null;
const scrim=document.createElement("button");scrim.type="button";scrim.className="mobile-detail-scrim";scrim.setAttribute("aria-label","Close selected record details");scrim.hidden=true;shell.append(scrim);
function close(returnToTrigger=true){if(!panel.classList.contains("is-mobile-open"))return;panel.classList.remove("is-mobile-open");panel.removeAttribute("role");panel.removeAttribute("aria-modal");scrim.hidden=true;document.body.classList.remove("mobile-detail-open");if(returnToTrigger){const target=returnFocus?.isConnected?returnFocus:document.querySelector("#graph-focus");if(typeof target?.focus==="function")target.focus()}returnFocus=null}
function open(trigger){if(!small.matches||!panel.dataset.selectedId)return;const closeButton=document.createElement("button");closeButton.type="button";closeButton.className="mobile-detail-close";closeButton.textContent="Close details";panel.prepend(closeButton);returnFocus=trigger&&typeof trigger.focus==="function"?trigger:document.activeElement;panel.classList.add("is-mobile-open");panel.setAttribute("role","dialog");panel.setAttribute("aria-modal","true");scrim.hidden=false;document.body.classList.add("mobile-detail-open");const heading=panel.querySelector("h3");if(heading){heading.id="graph-selected-heading";heading.tabIndex=-1;panel.setAttribute("aria-labelledby",heading.id);setTimeout(()=>{if(panel.classList.contains("is-mobile-open")&&heading.isConnected)heading.focus()},0)}}
panel.addEventListener("graph-selection-change",event=>{if(panel.classList.contains("is-mobile-open"))close(false);open(event.detail.trigger)});panel.addEventListener("click",event=>{if(event.target.closest(".mobile-detail-close"))close()});scrim.addEventListener("click",()=>close());tabs?.addEventListener("click",()=>close(false));small.addEventListener("change",()=>close(false));document.addEventListener("click",event=>{if(event.target.closest("[data-entity]")&&panel.classList.contains("is-mobile-open"))close(false)});
document.addEventListener("keydown",event=>{if(!panel.classList.contains("is-mobile-open"))return;if(event.key==="Escape"){event.stopPropagation();close();return}if(event.key!=="Tab")return;const controls=[...panel.querySelectorAll("button:not([disabled]),a[href],[tabindex]:not([tabindex='-1'])")].filter(element=>element.getClientRects().length);if(!controls.length)return;const first=controls[0],last=controls.at(-1);if(!panel.contains(document.activeElement)){event.preventDefault();(event.shiftKey?last:first).focus()}else if(event.shiftKey&&document.activeElement===first){event.preventDefault();last.focus()}else if(!event.shiftKey&&document.activeElement===last){event.preventDefault();first.focus()}})})();
`;
