---
"why-hire-me": patch
---

Fix isolated Firebase Hosting publication by selecting an explicit existing site ID instead of a
deploy target whose mapping is unavailable without the user's local Firebase project file.
